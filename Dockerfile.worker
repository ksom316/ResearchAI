FROM node:22-bookworm-slim

ENV NODE_ENV=production
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY tsconfig.json ./
COPY src ./src
COPY worker ./worker

USER node

# Override with `npm run worker:evidence` for the Evidence Matrix service.
CMD ["npm", "run", "worker"]
